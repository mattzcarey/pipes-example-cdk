export interface MessageBody {
  firstName: string;
  lastName: string;
  email: string;
  modifiedAttributes: string[];
}

export interface User {
  firstName: string;
  lastName: string;
  email: string;
}
