export interface MessageBody {
  firstName: string;
  lastName: string;
  email: string;
  changedParameters: string[];
}

export interface User {
  firstName: string;
  lastName: string;
  email: string;
}
